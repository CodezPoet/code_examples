<form method="post" action="{{ path('app_year_transaction_delete', {'id': year_transaction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ year_transaction.id) }}">
	<button class="btn btn-danger m-2">Delete</button>
</form>
